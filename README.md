# SliceFeatures

This repo takes the full features file and slices it according to features...

---

## Usage

Activate the venv

```shell
source .venv/bin/activate
```

Run main script:

```shell
python SLICE.py
```

The script will search in the `INPUT/` folder for two files: the propper `.csv` file and a names file (`.txt`).

...